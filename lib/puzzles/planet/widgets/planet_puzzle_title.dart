import 'package:flutter/material.dart';

/// title contains: planet name
/// and a random fact about that planet
class PlanetPuzzleTitle extends StatelessWidget {
  const PlanetPuzzleTitle({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
